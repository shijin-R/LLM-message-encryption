-r requirements-api.txt

# NVIDIA/CUDA 11.8 model service dependencies.
# Paddle 2.6 官方 Linux pip 文档将 CUDA 11.8 路线对应到 paddlepaddle-gpu==2.6.1。
paddlepaddle-gpu==2.6.1
paddlenlp==2.8.1
aistudio-sdk==0.2.6
# PaddleNLP 声明了该依赖，但上游只发布 Linux wheel；Windows 本地开发环境跳过。
tool-helpers==0.1.2; platform_system != "Windows"
